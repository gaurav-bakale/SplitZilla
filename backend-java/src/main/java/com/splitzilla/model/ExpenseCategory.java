package com.splitzilla.model;

/**
 * Enumeration of supported expense categories.
 * Used to classify and aggregate expenses by spending type.
 */
public enum ExpenseCategory {
    FOOD,
    ACCOMMODATION,
    TRANSPORT,
    ENTERTAINMENT,
    UTILITIES,
    SHOPPING,
    GENERAL
}
