# Application Configuration
spring.application.name=SplitZilla
server.port=8000

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://karans0811_db_user:XAEmhXQiPJhhDyCD@cluster0.vvzmrjp.mongodb.net/focusflow?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
jwt.secret=your-secret-key-change-in-production-make-it-at-least-256-bits
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173

# Jackson Configuration
spring.jackson.property-naming-strategy=SNAKE_CASE

# Logging
logging.level.com.splitzilla=DEBUG
logging.level.org.springframework.security=DEBUG
